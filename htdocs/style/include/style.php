<?php
/*
 * Theme settings
 */

// Ticket name/comment maximum length in the listings
$styleTicketMaxLen = 60;

// Ticket comment maximum line length in the listings
// (this forces word-break using a zero-width space).
$styleTicketLineLen = 45;

// Grant id/comment maximum length in the listings
$styleGrantMaxLen = 85;

// Grant comment maximum line length in the listings
// (this forces word-break using a zero-width space).
$styleGrantLineLen = 50;
?>